import 'package:flutter/material.dart';

class CameraRover {
  final Icon icon;              // icon bottom bar
  final String label;           // label bottom bar
  final String title;           // appbar and title info route
  final String description;     // description info route

  CameraRover({
    required this.icon,
    required this.label,
    required this.title,
    required this.description,
  });
}
